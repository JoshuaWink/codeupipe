"""
Prototype Metrics Toolkit — reusable product-level validation.

Point this at any deployed (or local) prototype and get a validation report
covering HTTP latency, bundle size, health polling, and overall grade.

This toolkit is itself a codeupipe pipeline (dogfooding).
"""
